<div class="container doodle">
  <div class="flex">
    <%= cl_image_tag @item.photo.key, class:"item-img" %>
    <div class="item-infos">
      <div class="sticky">
        <h1 style="font-weight: bold"><%= @item.name %></h1>
        <p><%= @item.description %></p>
        <h3 style="font-weight: bold"><%= @item.price %>0€</h3>
        <div class="doodle-button button-show">
        <%= link_to "ajouter au panier", create_cart_item_path(@item), class:"doodle-link"  %>
        </div>
      </div>
    </div>
  </div>
</div>