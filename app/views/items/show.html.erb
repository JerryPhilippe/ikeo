<div class="container-show doodle">
  <div class="flex">
    <%= cl_image_tag @item.photo.key, class:"item-img" %>
    <div class="item-infos">
      <div class="sticky">
        <h1 style="font-weight: bold"><%= @item.name %><%= image_tag("key.svg", id: "key" ,
                data: {
                action: "click->level-2#takeKey",
                level_2_target: "key"
              }) if current_user.current_game.level == 2 && @item.name.downcase == "brusalii"%></h1>
        <p><%= @item.description %></p>
        <h3 style="font-weight: bold"><%= @item.price %>0€</h3>
        <div class="doodle-button button-show">
        <%= link_to "ajouter au panier", create_cart_item_path(@item), class:"doodle-link", data: { action: card_view_action((current_user.current_game if user_signed_in?), @item)} %>
        </div>
      </div>
    </div>
  </div>
</div>
<%  %>
 <%= image_tag("home.png", id: "home-key" , class: "d-none",
                data: {
                action: "click->level-2#cave",
                level_2_target: "homekey"
              })%>
