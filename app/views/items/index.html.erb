<h1>Hello</h1>

<div class="grid">
<% @items.each do |item| %>
    <h1 class=""><%= item.name %></h1>

     <%= item.price %></p>
          <div class="">
            <%# if item.photo.attached? %>
              <%#= cl_image_tag item.photo.key, class:"item-img"%>
            <%# end %>

              <div class="display-name-and-score-flex">
                <h3 class=""><%= item.description %></h3>
              </div>
             <%= item.price %>€</p>
            <%= link_to "Voir cet item", items_path(item), class:"" %>
          </div>
        <% end %>
      </div>

</div>

  </div>
