<% room = @items.last %>


<div class="container">
<h1><%=room.room_type%></h1>

<div class="wrapper-index">
  <% @items.each do |item| %>
    <div class="card-index">
      <%= cl_image_tag item.photo.key, class:"item-img"%>
      <div class="card-index-text">
        <h1 class=""><%= item.name %></h1>
            <%= item.price %>0€</p>
              <%= link_to "Voir cet item", items_path(item), class:"" %>
      </div>
                </div>
  <% end %>
</div>

</div>
